spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:tqi-test-development
    username: application
    password:
    driver-class-name: org.h2.Driver

feign:
  hystrix:
    enabled: true
